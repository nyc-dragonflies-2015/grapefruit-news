<%= form_for [@post, @comment] do |f| %>
  <%= f.label :content %><br/>
  <%= f.text_area :content, cols: 40, rows: 7%><br/>
  <%= f.hidden_field :post_id, value: @post.id %>
  <%= f.hidden_field :user_id, value: the_signed_in_user.id %>
  <%= f.submit %>
<% end %>
